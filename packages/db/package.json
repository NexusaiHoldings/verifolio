{
  "name": "@nexus-substrate/db",
  "version": "0.1.0",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "build": "echo 'no build step for db package'",
    "migrate": "npx --no-install tsx migrate.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "drizzle-orm": "^0.30.0",
    "pg": "^8.11.0",
    "postgres": "^3.4.0",
    "tsx": "^4.19.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "@types/pg": "^8.11.0",
    "drizzle-kit": "^0.20.0",
    "typescript": "^5.4.0"
  }
}
