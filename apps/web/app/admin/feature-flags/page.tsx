/** /admin/feature-flags — renders the lego FeatureFlagsPage (substrate-admin-surface-001). */
"use client";

import { FeatureFlagsPage } from "@nexus/admin-console/ui/FeatureFlagsPage";

export default function Page(): JSX.Element {
  return <FeatureFlagsPage />;
}
