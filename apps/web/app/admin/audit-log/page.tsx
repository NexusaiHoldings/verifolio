/** /admin/audit-log — renders the lego AuditLogPage (substrate-admin-surface-001). */
"use client";

import { AuditLogPage } from "@nexus/admin-console/ui/AuditLogPage";

export default function Page(): JSX.Element {
  return <AuditLogPage />;
}
