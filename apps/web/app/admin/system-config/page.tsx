/** /admin/system-config — renders the lego SystemConfigPage (substrate-admin-surface-001). */
"use client";

import { SystemConfigPage } from "@nexus/admin-console/ui/SystemConfigPage";

export default function Page(): JSX.Element {
  return <SystemConfigPage />;
}
