# Files & Media — Configuration
`config_schema`: `storage.max_file_mb` + `storage.allowed_types`, `virus_scan.required`.
All tools read-only except `quarantine_file` (confirm — operator blocks a file).
