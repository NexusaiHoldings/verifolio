{
  "name": "@nexus/organizations-and-teams",
  "version": "1.0.0",
  "private": true,
  "description": "Multi-tenant orgs, invitations, RBAC, seats, audit log. Pre-built portfolio substrate capability per NEXUS_PORTFOLIO_RUNTIME_SPEC §11 #3.",
  "main": "./index.ts",
  "types": "./index.ts",
  "exports": {
    ".": "./index.ts",
    "./api/*": "./api/*.ts",
    "./ui/*": "./ui/components/*.tsx"
  },
  "scripts": {
    "build": "echo 'no build step — workspace package'",
    "typecheck": "tsc --noEmit",
    "test": "echo 'tests live in tests/ — run via root turbo test'"
  },
  "peerDependencies": {
    "next": "^14.2.0",
    "react": "^18.3.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "typescript": "^5.4.0"
  }
}
