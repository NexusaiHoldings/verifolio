{
  "name": "@nexus/support-and-help",
  "version": "1.0.0",
  "private": true,
  "description": "KB, in-app support widget, ticketing, AI deflection, feedback. The §12 AI-as-Backbone flagship — the agent runs first-line support. Pre-built portfolio substrate capability per NEXUS_PORTFOLIO_RUNTIME_SPEC §11 #7.",
  "main": "./index.ts",
  "types": "./index.ts",
  "exports": {
    ".": "./index.ts",
    "./api/*": "./api/*.ts",
    "./ui/*": "./ui/components/*.tsx"
  },
  "scripts": {
    "build": "echo 'no build step — workspace package'",
    "typecheck": "tsc --noEmit",
    "test": "echo 'tests live in tests/ — run via root turbo test'"
  },
  "peerDependencies": {
    "next": "^14.2.0",
    "react": "^18.3.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "typescript": "^5.4.0"
  }
}
