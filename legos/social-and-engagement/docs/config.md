# Social & Engagement — Configuration
`config_schema`: `moderation.auto_hide_spam` + `require_approval`,
`referrals.enabled` + `reward_type`. All tools read-only except `hide_comment`
(confirm — moderator hides user content).
