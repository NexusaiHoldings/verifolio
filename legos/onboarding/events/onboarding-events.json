{
  "lego_name": "onboarding",
  "version": "1.0.0",
  "description": "Typed event definitions for the onboarding lego (spec §7.6).",
  "events": [
    { "name": "onboarding.step_completed", "required": ["user_id", "step_key"], "properties": { "user_id": "uuid", "step_key": "string", "status": "string" } },
    { "name": "onboarding.step_updated", "required": ["user_id", "step_key"], "properties": { "user_id": "uuid", "step_key": "string", "status": "string" } },
    { "name": "onboarding.sample_data_requested", "required": ["run_id", "user_id"], "properties": { "run_id": "uuid", "user_id": "uuid" } }
  ]
}
